name: CI
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
jobs:
  build:
    name: ${{ matrix.os }}
    runs-on: ${{ matrix.os }}-latest
    strategy:
      matrix:
        os: ["ubuntu", "macos", "windows"]
    defaults:
      run:
        shell: bash -el {0}
    steps:
    - name: Check out
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Add pthreads for Windows
      if: runner.os == 'Windows'
      run: |
        echo "  - pthreads-win32" >> environment.yml

    - name: Show dependency file
      run: cat environment.yml
      
    - name: Install Mamba
      uses: conda-incubator/setup-miniconda@v3
      with:
        environment-file: environment.yml
        activate-environment: cycles
        auto-activate-base: false
        conda-remove-defaults: true

    - name: List environment
      run: |
        conda list
    
    - name: Configure and compile the library
      run: |
        cmake -DCMAKE_VERBOSE_MAKEFILE=yes -DCMAKE_INSTALL_PREFIX=$CONDA_PREFIX -B build
        cmake --build build --parallel

    - name: Run tests
      run: |
        cd build
        ctest --output-on-failure
